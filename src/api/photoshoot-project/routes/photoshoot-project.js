'use strict';

/**
 * photoshoot-project router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::photoshoot-project.photoshoot-project');
