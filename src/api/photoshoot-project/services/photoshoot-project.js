'use strict';

/**
 * photoshoot-project service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::photoshoot-project.photoshoot-project');
