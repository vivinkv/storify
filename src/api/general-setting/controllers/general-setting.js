'use strict';

/**
 * general-setting controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::general-setting.general-setting');
